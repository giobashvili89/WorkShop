{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // WARNING: This is a sample connection string. Update with your actual PostgreSQL credentials.
    // For production, use environment variables or secure configuration providers.
    "DefaultConnection": "Host=localhost;Port=5432;Database=workshop_db;Username=postgres;Password=postgres"
  },
  "Jwt": {
    // WARNING: This is a sample JWT secret. MUST be changed in production.
    // Use User Secrets in development: dotnet user-secrets set "Jwt:Secret" "your-secret-here"
    // Use environment variables or Azure Key Vault in production.
    "Secret": "YourSuperSecretKeyForJWTTokenGeneration12345",
    "Issuer": "WorkShopAPI",
    "Audience": "WorkShopClient"
  }
}
