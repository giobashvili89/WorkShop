# PostgreSQL Configuration
POSTGRES_DB=workshop_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# JWT Configuration
# IMPORTANT: Generate a strong random secret for production (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_A_RANDOM_BASE64_STRING_AT_LEAST_32_CHARS_LONG_USE_OPENSSL_RAND_BASE64_32
JWT_ISSUER=WorkShopAPI
JWT_AUDIENCE=WorkShopClient

# API Configuration
ASPNETCORE_ENVIRONMENT=Development

# Database Connection (for API)
ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=workshop_db;Username=postgres;Password=postgres
